[project]
name = "marketplaceonstyx"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "pydantic>=2.13.4",
    "sanic>=25.12.1",
    "sanic-ext>=25.12.0",
    "styx",
]

[tool.uv.sources]
styx = { workspace = true }

[tool.uv.workspace]
members = ["styx-package"]
